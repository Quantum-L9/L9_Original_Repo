# ============================================================================
# DORA META - AUTO-GENERATED
# ============================================================================
# component_name: "Factories Gmp Manifest Factory"
# module_version: "1.0.0"
# created_by: "Igor Beylin"
# created_at: "2026-01-02T15:15:57Z"
# updated_at: "2026-01-06T16:58:23Z"
# layer: "foundation"
# domain: "code_generation"
# file_name: "factories_gmp_manifest_factory"
# type: "factory"
# status: "active"
# ============================================================================

# =====================================
# GMP Manifest Factory (Autogenerator)
# =====================================

filename: factories/gmp_manifest_factory.py
type: gmp_generator
language: python

description: |
  Automatically generates GMP specs and Cursor-compatible prompt manifests
  from structured meta.yaml or capsule.json schemas.

wiring:
  inputs:
    - meta.yaml
    - manifest_template.yaml
  outputs:
    - gmp_manifest.yaml
    - gmp_prompt.md
  consumers:
    - CodeGenAgent
    - ResearchSwarmLoader
    - AI-CTO
    - ReflectionAgent

code: |
  import yaml
  import os

  def build_gmp_manifest(meta_path: str, output_dir: str):
      with open(meta_path) as f:
          meta = yaml.safe_load(f)

      manifest = {
          "agent_name": meta["name"],
          "goals": meta.get("responsibilities", []),
          "inputs": meta.get("inputs", []),
          "expected_outputs": meta.get("outputs", []),
          "style": "AI-CTO",
          "capsule_type": "GMP"
      }

      os.makedirs(output_dir, exist_ok=True)
      with open(os.path.join(output_dir, "gmp_manifest.yaml"), "w") as f:
          yaml.dump(manifest, f)

# ============================================================================
# DORA FOOTER - AUTO-GENERATED
# ============================================================================
# tags: ["agent-config", "code-generation", "factory", "foundation", "prompt", "schema"]
# keywords: ["code", "description", "factories", "factory", "filename", "gmp"]
# last_modified: "2026-01-06T16:58:23Z"
# ============================================================================
