# ============================================================================
# DORA META - AUTO-GENERATED
# ============================================================================
# component_name: "Values-Dev"
# module_version: "1.0.0"
# created_by: "Igor Beylin"
# created_at: "2026-01-25T03:21:35Z"
# updated_at: "2026-01-25T03:21:17Z"
# layer: "operations"
# domain: "deploy"
# file_name: "values-dev"
# type: "utility"
# status: "active"
# ============================================================================

# =============================================================================
# L9 Platform Helm Chart - Development Environment
# =============================================================================
# Usage:
#   helm install l9-dev ./l9-platform -f values-dev.yaml
# =============================================================================

global:
  environment: development
  domain: l9-dev.local

api:
  replicaCount: 1

  image:
    tag: "latest"
    pullPolicy: Always

  ingress:
    enabled: false

  resources:
    requests:
      memory: "512Mi"
      cpu: "250m"
    limits:
      memory: "1Gi"
      cpu: "1000m"

  autoscaling:
    enabled: false

  podDisruptionBudget:
    enabled: false

  env:
    LOG_LEVEL: DEBUG

mcpMemory:
  replicaCount: 1

  image:
    tag: "latest"
    pullPolicy: Always

  resources:
    requests:
      memory: "256Mi"
      cpu: "100m"
    limits:
      memory: "512Mi"
      cpu: "500m"

postgresql:
  primary:
    persistence:
      size: 5Gi

    resources:
      requests:
        memory: "512Mi"
        cpu: "250m"
      limits:
        memory: "1Gi"
        cpu: "1000m"

redis:
  master:
    persistence:
      size: 2Gi

    resources:
      requests:
        memory: "128Mi"
        cpu: "50m"
      limits:
        memory: "256Mi"
        cpu: "250m"

neo4j:
  persistence:
    size: 5Gi

  resources:
    requests:
      memory: "512Mi"
      cpu: "250m"
    limits:
      memory: "1Gi"
      cpu: "500m"

monitoring:
  prometheus:
    persistence:
      size: 5Gi

    retention: 7d

  grafana:
    persistence:
      size: 2Gi

    ingress:
      enabled: false

  jaeger:
    persistence:
      size: 2Gi

security:
  networkPolicy:
    enabled: false

backup:
  enabled: false
# ============================================================================
# DORA FOOTER - AUTO-GENERATED
# ============================================================================
# tags: ["api-config", "deploy", "operations", "redis-config", "utility"]
# keywords: ["api", "backup", "dev", "global", "mcpmemory", "monitoring"]
# last_modified: "2026-01-25T03:21:17Z"
# ============================================================================
