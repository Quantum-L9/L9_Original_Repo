# Deprecated Schema

This schema directory contains deprecated `memory.*` tables that are no longer used.

The MCP Memory Server now uses the unified L9 substrate:
- `packet_store` (from L9 migrations)
- `memory_embeddings` (from L9 migrations)

See: `migrations/0001-0013` for current schema.

**Status:** Archived for historical reference only. Do not use.
