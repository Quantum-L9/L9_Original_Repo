# MCP Memory Server - Canonical deps for Docker (root)
# ADR-0089: Single source at repo root. Dockerfile.mcp-memory uses this file.
# Local dev in mcp_memory/: from repo root: pip install -r requirements-mcp-memory.txt

# MCP Core
mcp>=1.0.0
pydantic-settings>=2.14.0

# FastAPI & async
fastapi>=0.136.1
uvicorn[standard]>=0.46.0
httpx>=0.27.0

# Database (async)
asyncpg>=0.29.0
psycopg[binary]>=3.1.0
pgvector>=0.2.5

# Embeddings & LLM
openai>=1.0.0
tenacity>=8.2.0
langgraph>=1.1.10

# Logging
structlog>=24.1.0

# System Monitoring (memory/consolidation adaptive batching)
psutil>=5.9.0

# Metrics (required for telemetry/memory_metrics.py)
prometheus_client>=0.19.0

# Utils
python-dotenv>=1.0.0
pyyaml>=6.0.1
aiofiles>=23.2.0
numpy>=1.24.0
tiktoken>=0.5.0

# Redis & Neo4j (required for MCP memory operations)
redis>=4.0.0
neo4j>=5.0.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
