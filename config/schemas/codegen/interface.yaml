# ============================================================================
# DORA META - AUTO-GENERATED
# ============================================================================
# component_name: "Interface"
# module_version: "1.0.0"
# created_by: "L9_Codegen_Engine"
# created_at: "2025-12-20T18:07:27Z"
# updated_at: "2026-01-20T23:43:16Z"
# layer: "foundation"
# domain: "current_work"
# file_name: "interface"
# type: "utility"
# status: "active"
# ============================================================================

schema_id: interface
schema_version: "1.0"
description: >
  Defines input/output contracts between modules and agents.
  Interfaces ensure compatibility and enable validation.

required_fields:
  - interface_id
  - name
  - fields

optional_fields:
  - description
  - version
  - required_fields
  - optional_fields
  - validation_rules

field_definitions:
  interface_id:
    type: string
    pattern: "^I-[A-Z_]+-[0-9]{3}$"
    description: "Unique identifier (e.g., I-CA_OUTPUT-001)"

  name:
    type: string
    description: "Human-readable interface name"
    examples:
      - "CA Output Contract"
      - "Verification Report Schema"

  fields:
    type: array
    description: "List of fields in this interface"
    items:
      type: object
      properties:
        name:
          type: string
        type:
          type: string
        required:
          type: boolean
        description:
          type: string

  description:
    type: string
    description: "What this interface is for"

  version:
    type: string
    pattern: "^\\d+\\.\\d+\\.\\d+$"
    description: "Semantic version"

  required_fields:
    type: array
    description: "Fields that must be present"

  optional_fields:
    type: array
    description: "Fields that may be present"

  validation_rules:
    type: array
    description: "Additional validation logic"

classification_hints:
  keywords:
    - interface
    - contract
    - schema
    - input
    - output
    - fields
    - structure

runtime_behavior:
  effect: "io_enforcement"
  enforcement: "schema validation"
  failure_action: "reject invalid data"

examples:
  - interface_id: I-CA_OUTPUT-001
    name: "CA Output Contract"
    version: "1.0.0"
    description: "Required output format for coding agent"
    fields:
      - name: diff
        type: string
        required: true
        description: "Unified diff patch"
      - name: report
        type: object
        required: true
        description: "Verification report"
      - name: confidence
        type: float
        required: true
        description: "Confidence score 0.0-1.0"
# ============================================================================
# DORA FOOTER - AUTO-GENERATED
# ============================================================================
# tags: ["agent-config", "current-work", "foundation", "schema", "utility"]
# keywords: ["classification_hints", "description", "examples", "field_definitions", "interface", "optional_fields"]
# last_modified: "2026-01-20T23:43:16Z"
# ============================================================================
