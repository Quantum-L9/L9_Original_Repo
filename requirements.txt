# L9 Core Dependencies
fastapi>=0.136.1
uvicorn[standard]>=0.46.0
pydantic>=2.13.3
python-dotenv>=1.2.3

# Pydantic Settings (separate package in v2)
pydantic-settings>=2.14.0

# Database
psycopg[binary]>=3.3.3
sqlalchemy>=2.0.49
asyncpg>=0.31.0
pgvector>=0.2.4

# Redis
redis>=7.4.0

# Neo4j
neo4j>=6.1.0

# HTTP Client
httpx>=0.26.0

# OpenAI / LLM
openai>=1.10.0

# LangGraph / LangChain
langgraph>=1.1.10
langchain-core>=1.3.2

# Logging
structlog>=24.1.0

# Async utilities
anyio>=4.2.0
aiofiles>=23.2.0

# Typing extensions
typing-extensions>=4.9.0

# Slack
# slack-sdk removed - using httpx-based async client (api/slack_client.py)

# Twilio (SMS/Voice)
twilio>=8.10.0

# Templates
Jinja2>=3.1.2

# YAML
PyYAML>=6.0.1

# Cryptography (signature verification)
cryptography>=41.0.0

# Symbolic Computation (Quantum AI Factory)
sympy>=1.12
numpy>=1.24.0

# Cross-encoder reranking for hybrid retrieval
sentence-transformers>=2.2.0

# Browser Automation (Mac Agent - local only, not Docker)
playwright>=1.58.0

# MCP (Model Context Protocol) - for mcp_memory server
mcp>=1.0.0

# System Monitoring (memory/consolidation adaptive batching)
psutil>=5.9.0

# Observability
prometheus_client>=0.19.0

# OpenTelemetry (Jaeger tracing)
opentelemetry-api>=1.41.1
opentelemetry-sdk>=1.21.0
opentelemetry-exporter-otlp-proto-http>=1.21.0

# JSON Schema Validation (used in event_type_registry, pattern orchestrator)
jsonschema>=4.21.0

# Retry Logic
tenacity>=8.2.3

# Testing & Linting (optional, for dev)
pytest>=9.0.3
pytest-asyncio>=1.3.0
pytest-cov>=7.1.0
pytest-mock>=3.15.1
ruff>=0.4.0
vulture>=2.16
mypy>=1.10.0
python-multipart
# Pin urllib3 to avoid LibreSSL warning on macOS
urllib3<3

# Mutation Testing
mutmut>=2.4.5

# ToTh Reasoning Engine
aiohttp>=3.9.0
networkx>=3.0

# Caching (for query result caching)
cachetools>=5.3.0

# WebSocket client (for e2e testing)
websockets>=12.0
