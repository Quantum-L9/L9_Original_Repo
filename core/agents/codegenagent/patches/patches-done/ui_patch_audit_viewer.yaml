# ============================================================================
# DORA META - AUTO-GENERATED
# ============================================================================
# component_name: "Ui Patch Audit Viewer"
# module_version: "1.0.0"
# created_by: "Igor Beylin"
# created_at: "2026-01-25T14:50:13Z"
# updated_at: "2026-01-25T08:49:00Z"
# layer: "operations"
# domain: "api_gateway"
# file_name: "ui_patch_audit_viewer"
# type: "router"
# status: "active"
# ============================================================================

# ============================
# Patch Audit Viewer (UI API)
# ============================

filename: ui/patch_audit_viewer.py
type: ui_module
language: python

description: |
  Displays all recent patches, prompt diffs, kernel overrides, and rollback states.
  Can be queried via API or used as a frontend module in L9’s cockpit.

wiring:
  source: patch_audit_log.yaml
  routes:
    - GET /api/patches
    - GET /api/patch/{agent_id}
  outputs:
    - JSON audit feeds
    - diff previews

code: |
  from fastapi import APIRouter

  router = APIRouter()

  @router.get("/api/patches")
  def list_patches():
      import os
      return os.listdir("patch_audit/")

  @router.get("/api/patch/{agent_id}")
  def get_patch(agent_id: str):
      path = f"patch_audit/{agent_id}_patch.yaml"
      with open(path) as f:
          return {"patch": f.read()}

# ============================================================================
# DORA FOOTER - AUTO-GENERATED
# ============================================================================
# tags: ["agent-config", "api-config", "api-gateway", "kernel", "operations", "prompt", "router"]
# keywords: ["audit", "code", "description", "filename", "language", "patch"]
# last_modified: "2026-01-25T08:49:00Z"
# ============================================================================
